import os
import sys
import requests

class down_url(url, headers, cookie):
    def __init__(url, headers, cookie):
        self.url = url
        self.headers = headers
        self.cookie = cookie

    def downurl(url)
        
